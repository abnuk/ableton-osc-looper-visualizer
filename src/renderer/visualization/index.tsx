import React from 'react';
import ReactDOM from 'react-dom/client';
import LooperVisualization from './LooperVisualization';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <LooperVisualization />
  </React.StrictMode>
);

